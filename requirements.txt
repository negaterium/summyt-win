# For GPU acceleration, ensure you have a compatible version of PyTorch installed.
# You may need to install a specific version depending on your CUDA toolkit.
# For example, for CUDA 12.9, you might run:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu129

torch
torchvision
torchaudio
yt-dlp
librosa
soundfile
nemo_toolkit[asr]
requests
configparser
matplotlib
scipy
nltk